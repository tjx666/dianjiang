{
  "name": "dianjiang",
  "version": "0.1.0",
  "description": "点将 — summon the right coding agent. Cross-vendor agent dispatch for Claude Code / Codex / Grok.",
  "type": "module",
  "license": "MIT",
  "bin": {
    "dianjiang": "./src/cli/index.ts"
  },
  "exports": {
    ".": "./src/core/index.ts"
  },
  "files": [
    "src"
  ],
  "scripts": {
    "check": "tsc --noEmit",
    "test": "bun test",
    "smoke": "bun run scripts/smoke.ts"
  },
  "dependencies": {
    "@clack/prompts": "^1.7.0",
    "citty": "^0.2.2",
    "jsonc-parser": "^3.3.1"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "typescript": "^7.0.2"
  }
}
